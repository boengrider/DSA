//The array is declared on the heap
//The array is initialized by the loader
//No instructions are used, data is already there when
//main starts running
//Refer to the heapArray.s;'

//Array A is declared and initialized
int A[5] = { 1, 2, 3, 4, 5 };
//Array B is only declared
int B[5];
int main() {}