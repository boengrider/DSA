{
    "tasks":[
        {
            "type": "shell",
            "label": "GCC - Compile active C file",
            //"command": "/usr/bin/gcc",
            "command": "/usr/bin/touch test",
            "args": [
                "${file}"
            ],
            "group": {
                "kind":"build",
                "isDefault": true
            }
        }
    ],

    "version":"2.0.0"
}